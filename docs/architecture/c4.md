# Retrosync Media Group — C4 Architecture

## System Context
```mermaid
C4Context
  Person(artist, "Artist/Label", "Uploads tracks, views royalties")
  System(platform, "Retrosync Platform", "BTFS · BTTC · ZK · DDEX · LangSec")
  System_Ext(bttc, "BTTC Testnet", "BitTorrent Chain")
  System_Ext(ddex, "DDEX Sandbox", "ERN 4.1")
  System_Ext(btfs, "BTFS Network", "Decentralised storage")
  System_Ext(wiki, "Wikidata", "Artist metadata SPARQL")
  Rel(artist, platform, "Uploads audio + metadata")
  Rel(platform, btfs,   "Stores audio CID")
  Rel(platform, bttc,   "Distributes BTT royalties + ZK proof")
  Rel(platform, ddex,   "Registers ERN 4.1")
  Rel(platform, wiki,   "Enriches artist metadata")
```

## Business Units
- **Retrosync Platform** — distribution + ZK royalties
- **Retrosync Records** — A&R
- **Retrosync Publishing** — sync licensing
